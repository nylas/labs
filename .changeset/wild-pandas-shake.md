---
"@ownmail/app": minor
---

Sign in from OwnMail's own login form instead of the Nylas-hosted credential screen. Credentials are posted server-side, sign-in attempts are rate-limited per mailbox and per client address, and every rejected credential returns one generic message.
