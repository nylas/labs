---
"ownmail": patch
---

Store pending setup secrets in the OS keyring when available, require inbox-password acknowledgement, add a pending-secret cleanup command, and improve the completion screen with login, recovery, and IMAP/SMTP guidance.
