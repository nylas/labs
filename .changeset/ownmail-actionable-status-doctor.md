---
"ownmail": patch
---

Make `status` and `doctor` more actionable: status now shows human-readable project state and JSON output, while doctor is read-only by default and gates redirect repairs behind `--fix`.
